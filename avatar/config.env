# =============================================================================
# Avatar Issuer VS — Configuration
# =============================================================================
#
# This file is read by the setup scripts and GHA workflow.
# It contains all configuration needed for the Avatar Issuer child service.
#
# For local testing:
#   source avatar/config.env
#   ./avatar/scripts/setup.sh
#
# =============================================================================

# ---------------------------------------------------------------------------
# Network (derived from branch name in GHA; set manually for local usage)
# ---------------------------------------------------------------------------
# NETWORK="testnet"

# ---------------------------------------------------------------------------
# Veranad CLI account name
# ---------------------------------------------------------------------------
USER_ACC="org-vs-admin"

# ---------------------------------------------------------------------------
# Organization — parent service
# Set to the public URL of the organization agent (K8s or local).
# The admin API is used to request Service credentials.
# ---------------------------------------------------------------------------
ORG_VS_PUBLIC_URL="${ORG_VS_PUBLIC_URL:-}"
ORG_VS_PUBLIC_URL="${ORG_VS_PUBLIC_URL%/}"
ORG_VS_ADMIN_URL="${ORG_VS_ADMIN_URL:-http://localhost:3000}"
ORG_VS_ADMIN_URL="${ORG_VS_ADMIN_URL%/}"

# ---------------------------------------------------------------------------
# VS Agent image and ports (local development)
# Use different ports than organization to avoid collision.
# ---------------------------------------------------------------------------
VS_AGENT_IMAGE="${VS_AGENT_IMAGE:-veranalabs/vs-agent:latest}"
VS_AGENT_CONTAINER_NAME="avatar"
VS_AGENT_ADMIN_PORT="3002"
VS_AGENT_PUBLIC_PORT="3003"

# ---------------------------------------------------------------------------
# Service details
# These appear in the Service credential issued by organization.
# ---------------------------------------------------------------------------
SERVICE_NAME="Avatar Issuer"
SERVICE_TYPE="IssuerService"
SERVICE_DESCRIPTION="Avatar credential issuer for EAFIT Challenge"
SERVICE_LOGO_URL="https://hologram.zone/images/icons8-avatar-48.svg"
SERVICE_MIN_AGE="0"
SERVICE_TERMS="https://verana.io/terms"
SERVICE_PRIVACY="https://verana.io/privacy"

# ---------------------------------------------------------------------------
# Custom schema base ID
# Must match the schema deployed by organization.
# ---------------------------------------------------------------------------
CUSTOM_SCHEMA_BASE_ID="avatar-eafit"

# ---------------------------------------------------------------------------
# AnonCreds
# Set to "true" to create an AnonCreds credential definition for issuance.
# ---------------------------------------------------------------------------
ENABLE_ANONCREDS="true"
ANONCREDS_NAME="avatar-eafit"
ANONCREDS_VERSION="1.0"
ANONCREDS_SUPPORT_REVOCATION="false"

# ---------------------------------------------------------------------------
# Chatbot configuration (for Step 3 — students implement their own)
# ---------------------------------------------------------------------------
CHATBOT_PORT="4000"

# Session persistence
# Local: SQLite file path
# K8s:   PostgreSQL connection string (e.g., postgresql://user:pass@host:5432/db)
DATABASE_URL="sqlite:./data/chatbot.db"

# Logging level (debug, info, warn, error)
LOG_LEVEL="info"

# ---------------------------------------------------------------------------
# MinIO — temporary object storage for avatar image previews
# ---------------------------------------------------------------------------
MINIO_ENDPOINT="localhost"
MINIO_PORT="9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="avatar-previews"
MINIO_USE_SSL="false"
# Public URL used to build presigned download URLs (set to ngrok or ingress URL)
MINIO_PUBLIC_URL="${MINIO_PUBLIC_URL:-http://localhost:9000}"
